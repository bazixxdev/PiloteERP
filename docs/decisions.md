# Décisions de conception

Une ligne par décision : date, décision, raison.

- 12/09/2026 — Barre latérale bleu profond avec texte clair, contenu sur fond crème. Raison : « bleu pour la navigation » dans la charte, contraste net avec le contenu.
- 12/09/2026 — Police locale « Avenir Next / Segoe UI » sans chargement distant. Raison : la maquette doit rester autonome.
- 12/09/2026 — La personne courante est stockée dans un cookie `pilote_person` ; par défaut la direction. Raison : sélecteur « Je suis… » sans authentification.
- 12/09/2026 — Les listes de statuts sont des tables (`RefValue` par famille) avec un code stable en anglais et un libellé modifiable en français. Raison : ENF-12, sans multiplier les tables.
- 12/09/2026 — L'historique des modifications (EF-B6) est un journal simple `ChangeLog` (édition, champ, avant, après, auteur, date). Raison : suffisant pour une vérification, pas de versionnage complet.
- 12/09/2026 — Les rappels (EF-C2) sont calculés à l'affichage depuis les livrables et jalons, pas stockés. Raison : pas de tâche planifiée dans un prototype ; `/rappels` les journalise à la volée.
- 12/09/2026 — Reconduction en N+1 : si une édition N+1 existe déjà, l'action est refusée avec un message. Raison : éviter les doublons sans corbeille.
- 12/09/2026 — Le contrôle de charge du séminaire compte les jours vendus de toutes les éditions de l'année cible (statut ≠ bilan fait). Raison : EF-B3b, calcul simple.
- 12/09/2026 — Export .docx basique via la bibliothèque `docx` (titres, paragraphes, tableau d'indicateurs). Raison : hors périmètre d'un export Word fidèle.
- 12/09/2026 — Aucune suppression depuis l'interface (actions, lignes, livrables, personnes) : on passe une action à « fait », une personne en « inactif ». Raison : EF-K1 (pas de suppression sans corbeille) ; la corbeille est hors périmètre du prototype.
- 12/09/2026 — La grille de temps propose les projets de l'équipe de la personne, ses propres actions non terminées et ses codes hors projet ; les autres actions du projet apparaissent seulement si un temps y est déjà saisi. Raison : rester sous la minute de saisie, sans lister 8 actions par projet.
- 12/09/2026 — Le temps « consommé » d'une édition = temps saisi sur le projet pendant l'année de l'édition (avec ou sans action). Raison : une édition n'a pas de dates de début et de fin.
- 12/09/2026 — Les relances de temps de la clôture sont simulées (badge « Relancé·e » sans persistance). Raison : brief, pas de mail dans le prototype.
- 12/09/2026 — Raccourcis clavier « g puis lettre » et « ? » pour l'aide, ⌘K pour la recherche. Raison : référence Linear, sans conflit avec les champs de saisie.
- 12/09/2026 — Vue par pôle (EF-G6) = la vue annuelle filtrée sur un pôle, avec un bloc « éditions du pôle et alertes » au-dessus. Raison : un écran de moins à maintenir, même données.
- 12/09/2026 — Le séminaire réutilise la reconduction unitaire pour chaque projet ; « ajuster » crée l'édition en statut « re-challengée », « arrêter » consigne la décision sur l'édition N sans rien créer. Raison : une seule logique de copie (EF-A2) pour l'unitaire et le lot (EF-A5).
- 12/09/2026 — Barre latérale réduite aux icônes sous 1024 px (responsive simple, sans version mobile). Raison : consultation sur tablette possible, mobile hors périmètre.
- 12/09/2026 — Relecture : la RAF n'est pas valideuse (niveaux 1 pilote / 2 responsable de pôle / 3 direction, comme le cahier des charges) ; un pilote ne décide que sur ses éditions, un responsable de pôle que sur son pôle, personne sur sa propre demande. Raison : EF-F2, et vérification côté serveur, pas seulement à l'affichage.
- 12/09/2026 — Relecture : la couche 3 est réservée au pilote (et à la direction) ; le responsable de pôle écrit le bilan et les champs « au fil de l'année » de son pôle, et modifie les actions de son pôle. Raison : brief partie 3 (droits), rôle de garant qui vise le bilan.
- 12/09/2026 — Simulation collective ajoutée en test Playwright (`tests/scenario-collectif.spec.ts`) : six personas sur une même édition, de la couche 1 à la clôture. Raison : les trois recettes sont mono-fil ; les interactions entre rôles méritent un test à part.
- 12/09/2026 — Aucune intégration tierce développée ; les points d'accroche sont documentés dans `docs/integrations.md` et attendent un go. Raison : hors périmètre du brief (partie 6), consigne 8.
- 12/09/2026 — Documents (EF-J1) : deux natures distinguées automatiquement — un chemin serveur (`\\cress\Partage\…`, NAS en VPN, ne s'ouvre pas depuis un navigateur : bouton « Copier le chemin ») et un lien web (Teams, OneNote, https). Le dossier de référence est proposé par convention depuis un gabarit réglé dans l'admin (`{code}`, `{annee}`). Raison : entretiens S4.34-47 (documents sur le NAS, OneDrive « rien sauf par erreur ») et S8.36-38 (canaux Teams par projet) ; pas de prise de position sur SharePoint avant la question 4.
- 12/09/2026 — Pièces jointes des validations : un chemin ou un lien en V0 ; l'upload léger dans l'outil (devis, justificatifs) est recommandé pour la V1 parce que le NAS a des droits ouverts et des suppressions accidentelles (S4.38-39). Non développé dans le prototype.
- 12/09/2026 — Go reçu : flux agenda iCal (sens outil → Outlook). Un flux par personne (`/api/agenda/{jeton}.ics` : mes jalons, livrables des projets que je pilote — tous pour la RAF —, mes validations à traiter) et un flux équipe (tous les jalons et livrables). Jetons non devinables, sans authentification (comme tout abonnement iCal). Format standard : aucune dépendance à Microsoft. Raison : EF-C2/ENF-8, entretiens S8.17-18, S8.28, S8.40 ; l'agenda reste l'agenda, l'outil n'y écrit pas.
- 12/09/2026 — La saisie assistée des temps depuis l'agenda (sens Outlook → outil, EF-D6) reste en V2 : elle exige la connexion Microsoft (question 4) et un cadre social (questions 5 et 12) ; elle devra rester une proposition relue par la personne. Raison : S4.97.
- 12/09/2026 — Go « gratuit ou presque » : (1) adresse de l'édition affichée pour un onglet Teams, (2) jeton d'API réglable dans l'admin, exigé sur les exports hors de l'outil (Excel « À partir du web »), les liens internes le portent ; (3) flux iCal public `/api/agenda/public.ics` des actions cochées « Public ». Raison : trois usages du corpus (canaux Teams, Excel RAF, refonte du site) couverts sans développement lourd ni dépendance.
- 12/09/2026 — Go pièces jointes typées : objet `Attachment` (nature en référentiel : devis, convention signée, notification, justificatif, bilan remis, compte rendu, autre), rattaché à l'édition et au besoin à une ligne de financement, un livrable ou une validation. 5 Mo au plus, PDF/image/Office/texte, stockage local `uploads/` (à remplacer par un stockage objet en V1), téléchargement réservé aux personnes connectées, pas de suppression ni de versions. Raison : le NAS a des droits ouverts et des suppressions vécues (S4.38-39) ; ce sont les pièces qui font foi, pas une GED (EF-J4).
- 13/09/2026 — Retours du cahier des charges v1-byGPT retenus et appliqués : (1) budget sans double comptage — objet `Expense`, devis approuvé → engagement une seule fois, réalisé rattaché par la RAF, disponible = enveloppe − réalisé − engagements restants ; (2) « en retard » = alerte calculée, l'état `late` disparaît ; (3) déclaration « cette semaine est complète » par la personne, annulée par toute correction, lue dans la clôture ; (4) décision de validation atomique et idempotente (double clic = une décision) ; (5) charge planifiée (`plannedDays`) distincte des jours conventionnés (`soldDays`) — 20 jours cofinancés restent 20 jours ; (6) écran café en visibilité collective (pas de nom de retardataire). Raison : corrections justes, faible coût, démo cohérente avec le cahier.
- 13/09/2026 — Rythmes de travail historisés : table `Rhythm` (heures par jour lundi→dimanche, semaine paire / impaire) et périodes d'effet par personne (`PersonRhythmPeriod`). Valeurs de l'accord d'entreprise (S3.66, S3.A.5) : option A 7,5 h lun-jeu + 6,5 h ven ; option B 8,25 h, 9 jours par quinzaine ; 80 % 28 h ; alternance 35 h. Saisie au pas de 0,25 h (nécessaire pour 8,25 / 6,5), heures décimales acceptées — à confirmer par l'arbitrage A05. Le rythme change dans le temps sans recalcul rétroactif ; à défaut de rythme : « référence hebdomadaire non configurée », jamais 35 h par défaut.
- 13/09/2026 — RH par édition en jours, pas en % ni en euros : par personne, jours prévus (charge, proposée par le pilote), jours conventionnés (référence financeur, RAF), jours réalisés (heures ÷ coefficient réglable, 7 h par défaut) ; le % du disponible et l'ETP sont affichés, jamais saisis. La valorisation (coût journalier, clés) reste dans l'Excel de la RAF (EF-E4). Raison : un % saisi se périme dès qu'un rythme change ; les financeurs demandent des jours ou des ETP, la RAF fait le reste.
- 13/09/2026 — Non retenus du v1-byGPT pour la V1 (trop lourds pour « simple à prendre en main », S8.43) : 9 statuts d'édition, 7 états de livrable, circuits de validation configurables, relevés d'indicateurs avec modes de calcul, constructeur de champs, corbeille relationnelle, revue trimestrielle photographiée. Reportés en V2 dans `evolutions.md`. La convention partagée (§10) est retenue pour la V1 mais non maquettée.
- 13/09/2026 — L'écran CODIR devient une page à part (`/codir`) au lieu d'un filtre du portefeuille : cinq blocs — validations en attente (décidables sur place), jalons dépassés, livrables financeurs proches, enveloppes en alerte, temps hors objectif — filtrables par pôle, mode projection, repère de séance 20 min. Chaque point a un bouton « Consigner » : objet `Decision` (instance CODIR / pôle / revue trimestrielle / CA, texte, suite confiée à, pour le), daté sur l'édition et visible dans son onglet Validations. Raison : EF-H2 (« alertes et validations seulement »), EF-F4 (décisions consignées), retour de Gaël « le mode CODIR ne fait rien de spécial ».
- 13/09/2026 — Retours à chaud (docs/RETOURS-A-CHAUD.md) : une entrée « Temps » avec trois onglets (ma saisie / temps de l'équipe / clôture) ; lien « Détail » de la clôture vers la grille d'une personne ; relance = notification dans l'outil (objet `Notification`, cloche, encart Ma semaine) tracée dans la clôture, mail en V1. Raison : nommage trompeur, action sans effet visible.
- 13/09/2026 — Rhabillage V2 sur la maquette `CRESS/maquette V2.html` (palette Pays de la Loire, Trebuchet MS en titres, rayons 6–8 px, barre latérale claire) ; détail dans `docs/charte.md`. Le corail ne sert plus aux boutons (réservé aux accents) ; les alertes passent en ocre / terre. Les dix entrées de navigation sont conservées et regroupées (Espace de travail / Collectif / Réglages) là où la maquette n'en montre que six ; la liste des pôles est un raccourci vers le portefeuille filtré. Raison : direction graphique validée par Gaël ; aucun changement de périmètre fonctionnel.
- 13/09/2026 — Logo : Gaël veut le vrai logo CRESS (pas le wordmark texte de la maquette). Le PNG fourni (225 × 85) était trop petit pour Retina : découpe du JPG 500 × 500 et suppression du fond blanc → `public/logo-cress.png` 465 × 187 transparent, plus la marque seule pour les écrans réduits.
- 13/09/2026 — Mobile (chapitre 06 de la maquette) : navigation basse à trois entrées et saisie des temps une journée à la fois (mêmes données et même sauvegarde que la grille), sans application dédiée. Mode projection (café, CODIR) : plein écran natif demandé, Échap ramène à l'écran normal.
- 13/09/2026 — Périmètre par pôle : chaque écran collectif (portefeuille, validations, rappels, vue annuelle, écran CODIR pour un responsable de pôle) s'ouvre sur **mon pôle** — les projets dont le pôle principal ou un pôle associé est le mien, plus les éditions où je pilote ou contribue — avec une bascule « Toute la CRESS ». Direction, RAF et assistante (transversales) voient tout d'emblée. Une édition hors de mon pôle se consulte avec un bandeau « hors de votre pôle », sans écriture. Raison : demande de Gaël — le quotidien d'une personne, c'est son pôle ; le reste se consulte, ce n'est pas sa vue par défaut (EF-G6).
- 13/09/2026 — Projet à pôle principal (celui du pilote) et pôles associés (`ProjectPole`) pour les projets communs : les membres des pôles associés le voient dans leur périmètre, le responsable d'un pôle associé y a ses droits de garant (bilan, actions, validations niveau 2). Réglé dans l'admin (puces à côté du pôle). Raison : projets communs (Forum, Sensibilisation, Communauté des financeurs) ; un seul projet, pas de copie par pôle.
- 13/09/2026 — Priorité d'affichage : dans le portefeuille, la file des validations, les rappels, la vue annuelle et la recherche ⌘K, ce qui me concerne vient d'abord — **je pilote**, puis **je contribue** (équipe, action, garant), puis **mon pôle**, puis les autres pôles — et, à pertinence égale, les alertes fortes. Le portefeuille affiche ces groupes en sous-titres pour une personne de pôle ; la direction, la RAF et l'assistante gardent la liste plate. Raison : demande de Gaël — « avant tout les projets où je suis nommé directement, ceux de mon pôle ensuite ».
- 13/09/2026 — Convention partagée (EF-C3, CDC GPT §10) : objet `Convention` (financeur, référence unique, dispositif, période en années, statut, demandé, notifié, dates) ; les lignes de financement des éditions deviennent ses affectations (`FundingLine.conventionId`). Règle : la somme des montants obtenus affectés ≤ notifié, sinon refus avec l'écart. Page `/conventions` (reste à affecter, éditions couvertes, prochaine obligation). La reconduction garde le rattachement si la convention couvre N+1 (ligne « conventionnée », montants à affecter) ; un financement annuel repart « à déposer ». Les livrables restent portés par la ligne de chaque édition (un livrable commun à deux éditions reste V1+). Raison : FSE 2026-2028 et CPO Région existent une fois ; le drapeau « pluriannuel » ne suffisait pas.
- 13/09/2026 — Correction : les dates s'affichent en date locale dans les champs (un 15/10 minuit Paris apparaissait 14/10 via UTC).
- 13/09/2026 — Mise en ligne de la démo sur le VPS Bazixx (`bazixx-vps`, jamais le VPS client MCA) : `https://cress.bazixx.fr/outilcli/cress/pilote` (convention outilcli, basePath Next), service systemd sur 3002, nginx avec rideau `auth_basic` + `noindex` + page de maintenance, certificat Let's Encrypt, SQLite et pièces sur disque, `deploy/deploy.sh` (build avant bascule, sauvegarde, migrations, seed si base vide, healthcheck, retour arrière). Les flux iCal restent accessibles sans mot de passe (leur jeton fait office de secret) pour qu'Outlook puisse s'y abonner. Les logos passent en `<img>` : l'optimiseur d'images Next ne suit pas le basePath.
- 13/09/2026 — Revue UI/UX (`CRESS/audit-ux/changements-recommandes.md`, 12 points) traitée dans l'ordre conseillé ; détail par point dans `RETOURS-A-CHAUD.md` (section F). Décisions de fond : (1) **budget** — le pourcentage écrit n'est jamais plafonné, seule la barre s'arrête à 100 %, et la formule « réalisé + engagements restant à réaliser » est la même partout (portefeuille, en-tête, Budget, CODIR, alertes) avec le dépassement écrit en euros ; (2) **clôture** — la tolérance cachée de 90 % est supprimée : « complet » veut dire que chaque jour attendu du rythme porte une saisie, et l'écran sépare couverture des saisies, déclaration de la personne et verrouillage ; le verrouillage collectif liste les personnes avant d'agir. **À confirmer avec la RAF** (tolérance voulue ? jour attendu vs heures attendues ?) avant de figer ; (3) **navigation par profil** — un salarié (pilote, contributeur, assistante) voit Ma semaine, Temps, Mes projets, Validations puis les accès « Toute la CRESS » ; CODIR et Séminaire n'apparaissent qu'aux rôles CODIR, Admin à la direction et à la RAF ; les compteurs de la barre ne comptent que ce que la personne doit traiter elle-même (validations qu'elle peut décider, rappels qui la nomment). Aucune faille de droits n'a été constatée : c'est une question de lisibilité ; (4) **fiche en lecture d'abord** — texte compact, « Modifier cette couche » pour le propriétaire (une couche vide s'ouvre directement en saisie), historique replié ; (5) **CODIR** — un ordre du jour de 5 sujets au plus, alertes regroupées par édition et notées (retards et dépassements pèsent plus), avec problème / décision attendue / responsable / échéance ; toutes les alertes restent en dessous, repliées en projection ; (6) **demande de validation** — pièce jointe avant l'envoi, destinataire réel nommé avec la raison du niveau, niveau et délai en paramètres secondaires (toujours modifiables, EF-F2).
- 13/09/2026 — Menu utilisateur (en haut à droite) à la place du simple sélecteur « Je suis… » : en-tête nom / rôle / pôle, **Mon compte** (`/compte` : identité, rôle, pôle, rythme, jours disponibles, codes de temps, projets pilotés, flux agenda — en lecture, les réglages restent dans l'admin), **Admin** (direction et RAF seulement ; l'entrée disparaît de la barre latérale), **Changer d'utilisateur** (modale de recherche par nom, rôle ou pôle, marquée « proto ») et **Se déconnecter**. Sans authentification réelle, la déconnexion ouvre la même modale avec l'explication ; en V1, retour à la connexion Microsoft. Raison : demande de Gaël, menu « habituel » d'une application.
- 13/09/2026 — Conventions en liste + page : `/conventions` devient un tableau (financeur · référence, période, statut, notifié, affecté · reste avec jauge, éditions couvertes, prochaine obligation ; filtres financeur / statut / type) et chaque convention a sa page `/conventions/[id]` (quatre montants, affectations aux éditions, informations modifiables par la RAF, obligations à venir). La création passe par une modale « Nouvelle convention » qui ouvre la page créée. Raison : demande de Gaël — les cartes-formulaires empilées ne permettaient ni de comparer ni de lire.
- 13/09/2026 — **To-do personnelle et vue journée** (go de Gaël). Objet `Task` (personne, libellé, échéance facultative, fait, rattachement facultatif à une édition ou une action) et `WorkSlot` (créneau posé sur une tâche : journée entière ou plage horaire, ex. 23/09 de 9 h à 11 h). Deux notions distinctes et toutes deux à l'écran : l'**échéance** dit « pour quand », le **créneau** dit « quand je m'y mets ». La personne n'est jamais un champ visible : une tâche appartient toujours à la personne connectée, personne d'autre ne la voit. Dans Ma semaine : bloc **Aujourd'hui** (créneaux du jour, tâches et échéances du jour, heures saisies face au rythme) et panneau **Mes tâches** (ajout en une ligne, échéance avec raccourcis aujourd'hui / demain / lundi, « Planifier » un créneau, terminer, supprimer ; les tâches datées en retard rejoignent « En retard »). Depuis une édition : « Me créer une tâche » (rattachée, avec action au choix). Flux agenda personnel : l'échéance en journée entière, chaque créneau en plage horaire **occupée** (TRANSP:OPAQUE) — c'est ainsi que le créneau « se pose dans l'agenda » Outlook, sans écriture Microsoft. Ce n'est ni une action de projet ni un jalon : pas de suivi collectif, pas de temps saisi automatiquement.
- 13/09/2026 — **Financeurs et contacts** (go de Gaël, option complète). Objet `FunderContact` (prénom, nom, fonction, email, téléphone, note, contact principal) ; `Funder.notes` ; `FundingLine.contactId` et `Convention.contactId` = contact du dossier, sinon le contact principal du financeur s'applique. Écrans : `/financeurs` (liste : contact principal, conventions actives, notifié, éditions financées, prochaine obligation) et `/financeurs/[id]` (contacts modifiables en place par la RAF avec étoile « principal », conventions, éditions financées, notes, obligations à venir). Le contact se lit en clair (nom · fonction, email et téléphone cliquables) sur chaque ligne de financement et sur la page de convention. Données personnelles de tiers : le minimum utile, tenu par la RAF, jeu de démo inventé (`@exemple.fr`) ; pas d'envoi de mail ni de synchronisation Outlook des contacts (question V1 Microsoft, RGPD). L'entrée « Financeurs » remplace l'usage du référentiel admin pour les créer.
- 13/09/2026 — Une entrée « Projets et financements » dans la barre latérale, avec trois onglets internes : **Projets et éditions** (`/projets`, sorti de l'admin ; `/admin?section=projets` redirige), **Conventions**, **Financeurs**. Le référentiel « Financeurs » de l'admin devient un simple renvoi (plus de doublon). Garde-fous sur « Actif » : on ne se désactive pas soi-même, il reste toujours une direction active ; et la personne courante retombe sur n'importe quelle personne active (puis la première) au lieu de planter quand le cookie est périmé après un reseed. Raison : demande de Gaël (regroupement, doublon, plantage constaté).
- 13/09/2026 — **Fiche projet alignée sur le gabarit Word « À COPIER — FICHE PROJET »** (go de Gaël) : rubriques ajoutées — objectifs qualitatifs (l'ancien « objectifs opérationnels », clé inchangée) et objectifs quantitatifs, lien SNESS, autres textes de rattachement, contenu développé / valeur ajoutée, public et bénéficiaires, sponsor (membre du CODIR), date de rendu ; libellés repris du gabarit (impacts attendus, lieux et animation, matériel-outils-mobilier-services, justificatifs et échéances). Export Word de la fiche au format du gabarit (`/edition/[id]/export?format=fiche`), remarques ouvertes incluses en italique. **Remarques par rubrique** (`FieldRemark`) : la direction, la RAF ou le responsable de pôle (garant ou pôle associé) pose une remarque sous une rubrique, comme un commentaire Word ; le pilote et l'équipe la lisent en place, la marquent traitée (ou la rouvrent) ; compteur par couche et bandeau sur la fiche ; notification au pilote et encart « N remarques à traiter » dans Ma semaine ; trace dans l'historique. Jeu de démo : trois fiches 2026 remplies à partir des fiches réelles (Refonte du site → COM-02, Note d'opportunité → TES-03, ASER → COO-02), textes transposés sans personne réelle ; trois remarques inventées de Claire Vasseur (personnage fictif) sur la fiche ASER — pas les commentaires réels du document, à la demande de Gaël. Raison : Gaël constatait que l'outil ne reproduisait ni toutes les rubriques ni les commentaires de la direction sur les fiches.
- 13/09/2026 — Remarques sur la fiche : un **droit du CODIR** (direction, RAF, responsables de pôle, quel que soit le pôle — la relecture des fiches se fait en CODIR), activé par un **mode relecture** (« Relire et annoter », paramètre `relecture=1`) pour ne pas parasiter la lecture : hors de ce mode, seules les remarques posées sont visibles. Raison : retour de Gaël — les boutons « Remarque » sous chaque rubrique alourdissaient la fiche.
- 13/09/2026 — **Plan de charge** (`/plan-de-charge`, go de Gaël) : grille personnes × mois (6, 12 ou 18 mois glissants, filtre pôle, vue par personne ou par projet, éditions en cours / à venir / à venir seulement). Objet `PlannedLoad` (édition, personne, mois, jours) : la ventilation mensuelle se pose depuis l'onglet Temps de l'édition (« Répartir par mois » : un total et une période lissés, puis ajustables mois par mois) et sa somme devient le total annuel `plannedDays` ; sans ventilation, le total annuel se lisse sur les 12 mois (marqué « lissé »). Capacité du mois = jours disponibles de l'année (admin, congés déduits) × part du mois dans les jours attendus du rythme. Ocre ≥ 85 %, terre > 100 %, bandeau des dépassements, réalisé (heures ÷ coefficient) sur les mois passés, détail par édition au clic. Hors périmètre : congés individuels (SIRH), affectation par action. Raison : rien ne répartissait la charge dans le temps entre le séminaire (annuel) et la saisie (réalisé).
- 13/09/2026 — Plan de charge modifiable depuis la grille (retour de Gaël : aller dans chaque édition n'est pas pratique quand on travaille la charge globale) : dans le détail d'une cellule, chaque édition a son champ de jours (enregistré à la sortie du champ), et « + Ajouter une édition » affecte la personne à une édition de l'année (elle rejoint l'équipe et la ligne RH si elle n'y était pas). Droits par édition : son pilote, la RAF, la direction, les responsables de pôle. Modifier un mois d'une affectation lissée matérialise d'abord les 12 mois lissés, puis remplace le mois ; le total annuel suit.
- 14/09/2026 — **Lot 1 « Mon travail »** (retour oral de Gaël après les entretiens du 14/09, `RETOURS-A-CHAUD.md` §H). (1) Listes de tâches (`TaskList`) : catégories avec ou sans projet ; **visibilité** private / pole_lead / pole / all choisie par l'auteur ; seul l'auteur écrit, les autres lisent (`/taches`, section « Partagées avec moi », `lib/modules.ts:canReadShared`). Supprimer une liste renvoie ses tâches dans « Sans liste ». (2) Notes (`Note`) : texte, date, contexte (projet, équipe, pôle, café, partenaire, libre), édition facultative, visibilité private / pole / all ; enregistrement à la volée en quittant un champ ; listées dans l'onglet Documents de l'édition. (3) **Modules** par personne (`Person.modules`, défaut tasks,notes,split) réglés dans Mon compte : un module coupé disparaît de la barre latérale et de Ma semaine, les données restent. (4) **Parts de ma semaine** : la personne répartit 100 % des heures attendues de son rythme entre ses lignes (curseurs, camembert) ; l'enregistrement remplace les saisies de la semaine sur ces lignes et pose des heures au prorata des heures attendues de chaque jour, au pas de 0,25 h, le reste d'arrondi sur le dernier jour ; l'export de la RAF ne change pas. Vocabulaire : « répartition », « clé de répartition, pas un pointage » — le temps de travail effectif et les heures supplémentaires restent hors outil. (5) **Part fixe** (`Person.fixedShare`, note) : personne sous lettre de mission à pourcentage fixe (règlement (UE) 2021/1060 art. 55 §5, à confirmer avec le gestionnaire FSE) — pas de grille, hors relances et hors clôture (sauf heures existantes). (6) **Aide à la saisie** (`lib/traces.ts`) : tâches terminées, créneaux posés, notes et jalons de la semaine, regroupés par projet ; un rappel de ce sur quoi on a travaillé, jamais une durée ; les projets concernés ont leur ligne dans la grille. (7) **Mode focus** (`components/common/focus-mode.tsx`, `?focus=1`) : la navigation disparaît (classe `focus-mode`), Échap ramène ; pas de plein écran natif, contrairement à la projection. Disponible sur la répartition du temps, une note, la fiche. Raison : sept demandes du retour oral, toutes vérifiables en live par un profil de la CRESS avant le lot 2.
- 14/09/2026 — **Lot 2 « La fiche et son cycle »** (`LOTS-14-09.md`). (1) **Verrouillage** : une fiche est verrouillée quand son cycle de validation est terminé — décision du CODIR posée en couche 4, ou statut validée / clôturée (`lib/lock.ts`) ; une édition « en cours » sans décision reste ouverte (les recettes créent l'édition puis passent « en cours » avant que le pilote écrive). Couches 1 à 3 en lecture repliée (résumé d'une ligne, « Déplier » ; dépliées d'office en relecture ou quand une remarque attend), refus côté serveur de toute écriture directe. **Proposition de modification** (`ChangeProposal`) : rubrique, nouvelle valeur, motif obligatoire ; pilote, équipe et CODIR proposent ; le pilote ou la direction décide (jamais l'auteur seul, sauf direction) ; accepter applique la valeur et écrit l'historique ; notifications au pilote, au garant, à la direction, puis à l'auteur. La direction rouvre en effaçant la décision CODIR ou en repassant à « proposée » — tracé. (2) **Proposition de projet** (`proposeProject`) : n'importe qui ; code `PROP-nn`, pôle de la personne, garant = responsable du pôle, première édition « proposée » avec le résumé en objectifs, le proposant pilote et seul membre ; responsable de pôle et direction notifiés. (3) **Réalisations** (`Achievement`) : nature (participants, public, livrable, retombée, partenaire, autre), libellé, chiffre facultatif avec unité, date, action liée ; totaux par nature dans l'onglet Bilan ; reprises dans les exports bilan (.md, .docx) et dans la fiche au gabarit. (4) **Plan opérationnel assemblé** (`lib/fiche-docx.ts` partagé avec l'export d'une fiche) : toutes les éditions non clôturées d'une année, par pôle puis mission, sommaire, une fiche par page ; ouvert depuis l'outil sans jeton, comme l'export d'une fiche. (5) **Plan de charge figé** (`LoadFreeze` par année) : direction ou RAF ; ensuite toute modification (grille ou ventilation) reste possible mais écrit un `ChangeLog` (`plannedLoad:{personne}`) et notifie RAF et direction ; l'écran liste les modifications après validation et marque les personnes concernées. (6) **Jours de fonctionnement** (`Settings.operatingDaysPerMonth`, 1,5 j dans la démo) déduits de la capacité mensuelle au prorata du rythme (S14 : 9 j sur 86). (7) **Motif des remarques** (`FieldRemark.reason`) : financeur / stratégie / faisabilité / forme / autre, affiché en badge et dans la notification (S14 : « je ne sais pas d'où ça vient »). (8) **Occurrences d'action** : `description`, `venue`, `participants` sous chaque action, « dupliquer » copie la petite fiche sans le jalon (S13 : un petit-déjeuner ORESS par fiche). (9) Démo : projet « Vie statutaire (CA, bureaux, AG) » REP-04 porté par la direction, actions tenues par l'assistante, dans le flux agenda équipe (S15 : agenda « vie interne »). Le champ « sponsor » attend l'arbitrage 1.
- 14/09/2026 — **Lot 3 « Demandes, devis, factures, documents »** (`LOTS-14-09.md`). (1) **Demandes** (`Request`) : type (site, com, chiffres, logistique, travail, autre), destinataire une personne ou un pôle (son responsable est notifié), pour quand, projet facultatif ; états à traiter / en cours / faite / déclinée, réponse au demandeur, réattribution ; « en faire une tâche » crée la tâche dans ma liste avec l'échéance et passe la demande en cours. La page `/demandes` **unifie** demandes et validations (même tableau, trois vues), l'achat / devis restant une validation. Compteur de barre = demandes qui me sont adressées ou adressées à mon pôle. (2) **Devis** : `supplier` et `supplierEmail` sur la demande ; à l'approbation, notification au demandeur avec le **bon pour accord** (`/validations/[id]/bon-pour-accord`) — mail prêt (accord, montant, référence, adresse de facturation `Settings.billingEmail` et consigne), copie ou `mailto:` ; l'envoi réel est V1 ; un accord écrit par une personne habilitée vaut acceptation, la signature électronique se réserve aux conventions (V1 optionnel). La direction reçoit « pour information » tout devis approuvé sans elle. (3) **Facture** : sur l'engagement, `invoiceReceivedAt` / `paidAt` posés par la RAF, la direction ou l'assistante ; `serviceDoneAt` par le pilote ou l'équipe, **non bloquant** ; `nature` achat / investissement / prestation par la RAF ; notification au pilote à la réception (avec la question du service fait) et au paiement ; pas de fichier facture dans l'outil, elle arrive à l'adresse de facturation. (4) **Documents** : règle écrite dans l'onglet (naît dans l'outil / fait foi / tout le reste sur le serveur ; Teams éphémère, OneDrive arrêté) ; pas de GED. Raison : circuit scanner / tamponner / signer jugé « useless » (Gaël), Sandrine veut un seul canal, Simon veut savoir que la facture est payée.
- 15/09/2026 — **Page Édition réorganisée par rythme d'usage** (revue `CRESS/audit-ux/revue-edition-2026-09-15.md`, go de Gaël sur toutes les préconisations). Six onglets : Aperçu (atterrissage une fois la fiche validée), Fiche (document en chapitres : couches, indicateurs, décisions d'instance, bilan, logistique, équipe, historique), Actions (+ réalisations et indicateurs vivants), Budget (dépenses puis recettes / financeurs), Temps, Documents ; Fil en panneau ; menu « … » pour l'annuel. Six règles d'écran : lecture d'abord, créer = un bouton, une phrase d'aide au plus (le reste en « ? »), une alerte se dit une fois et s'éteint quand une décision la règle (`Decision.alertKind`), le rythme fait la place, rien de tronqué (tableau + panneau latéral). Indicateurs = un seul objet (cible à la rédaction, réalisé dans l'année) ; les deux champs texte des couches 2 et 3 ne se saisissent plus. Date du CODIR = `codirDate` seul ; `decisionDate` reste en base sans écran. Raison : la page avait été construite par accumulation (lots des 13 et 14/09) ; l'annuel et l'hebdomadaire se retrouvaient au même niveau.
- 15/09/2026 — **Échéances et notifications sont deux objets distincts.** `/rappels` devient `/echeances` (radar collectif des livrables et jalons à venir, calculé à la volée, rien à « lire ») ; la cloche est la boîte de réception personnelle et gagne un centre `/notifications` (historique, filtres par famille, tout marquer comme lu). Une passerelle génère la notification chez le pilote (et la RAF pour un livrable) quand une échéance franchit J-30, J-7 ou passe en retard — une seule par palier (`Notification.dedupeKey`), datée du jour d'envoi théorique : c'est le rôle que tiendra le mail en V1. Raison : question de Gaël — deux écrans portaient la même icône et le même texte « à la place d'un mail » sans qu'aucun ne soit un vrai centre de notifications ; nommer chaque chose par ce qu'elle est et relier les deux dans un seul sens (échéances → cloche).
- 15/09/2026 — **Versements (lot A)** : un versement se rattache à une **ligne de financement** d'abord (cas courant) et à une **convention** seulement quand l'accord se verse par tranches indépendamment des éditions (FSE 2026-2028, CPO Région) ; les deux ne se cumulent pas sur un même euro (une ligne conventionnée sans versement propre affiche « sur convention »). Le statut n'est **jamais stocké** (reçu = `receivedAt` posé ; en retard = date attendue passée). « Reçu » est posé par la RAF **ou la direction** (Gaël : mêmes droits), jamais par un import ou un rapprochement — un rapprochement comptable (lot D) ne fera que proposer. Un versement reçu ne se supprime pas (retirer la réception d'abord). Pas de palier J-30 / J-7 pour un versement (rien à faire avant la date) : seul le retard notifie, la RAF et la direction. Raison : trou fonctionnel entre « notifié » et « justifié » relevé dans l'analyse `erp-tlst` ; règle reprise de TLST.
- 15/09/2026 — **Modules par installation (lot 0)** : `Settings.modules` à côté de `Person.modules`. Un module d'installation est un bloc entier (la veille aujourd'hui ; demain temps, validations, adhérents…) ; un module par personne est un confort individuel (tâches, notes, parts). Éteindre = disparaître de la navigation + « introuvable » sur les adresses ; jamais de suppression de données. Raison : première règle de `produit.md` (un client active ses modules), demandée par Gaël pour la veille.
- 15/09/2026 — **Appels à projets (lot B)** : le **statut d'équipe** (à étudier / on dépose / écarté) est une donnée de la CRESS, séparée du contenu de l'appel ; il se pose en CODIR (direction, RAF, responsables de pôle). « Étudier » crée une **convention** `to_submit` (pas une ligne de financement : à ce stade on ne sait pas encore quelle édition portera le dossier), une seule fois par appel ; le lien appel ↔ convention reste. Un appel n'est jamais supprimé (retiré = `active=false`). Le radar ne montre un appel que « on dépose » et sans convention : ensuite c'est la convention qui vit. Pas de notification en cloche pour les appels dans ce lot (le radar suffit ; à revoir avec la RAF). Raison : règles d'`erp-tlst` (module Veille) transposées ; convention = l'objet qui suit le dépôt.
- 15/09/2026 — **Matrice « Qui finance quoi » (lot C)** : lecture pure des lignes de financement, rien ne s'y modifie (on renvoie à l'onglet Budget de l'édition). Une cellule vaut **obtenu** dès qu'un montant est accordé, **demandé** dès qu'un dossier est parti (statut ≠ à déposer), sinon **à déposer** avec le montant prévu. La **couverture** compare l'obtenu à l'enveloppe de dépenses directes : ocre sous 90 %, vert au-dessus, **jamais rouge au-delà de 100 %** — la subvention finance aussi les jours vendus, donc dépasser l'enveloppe n'est pas une anomalie (le seed le montre partout). Montants réservés au CODIR (direction, RAF, responsables de pôle), pastilles pour les autres ; export CSV réservé aux mêmes. Raison : les données existaient édition par édition et financeur par financeur, aucune page ne les croisait (constat de Gaël) ; patron du `/pilotage` d'erp-tlst.
- 15/09/2026 — **Lexique** : un seul fichier (`lib/lexique.ts`), un dialogue, trois points d'entrée (matrice, portefeuille, projets). La **ligne de financement** est le cas courant, la **convention** l'exception (pluriannuel ou multi-projets) : c'est dans ce sens que l'outil se lit. À valider avec la CRESS le 28/09 (question 1).
- 15/09/2026 — **Réalisé comptable (lot D)** : la compta est une **source externe** lue en snapshot, jamais une table reliée : `LedgerLine` n'a aucune clé étrangère, le rapprochement passe par le **code analytique** (ligne de financement de l'exercice, sinon projet, sinon correspondance posée à la main) ; un code qui se répète chaque année (FSE-26, FSE-27) se résout sur la ligne dont l'édition est de l'exercice. **Une seule source compte** dans les alertes d'enveloppe et le portefeuille (`Settings.realizedSource`) ; l'autre s'affiche à côté, jamais additionnée. Le fichier importé est la base (marche avec n'importe quel logiciel) ; Pennylane est un connecteur optionnel par configuration serveur ; « Excel en direct » n'est pas codé tant que Gaël n'a pas précisé (chemin fixe relu ? feuille publiée ?). Les **notes de frais** importées se lisent sur les comptes 625 (lot G, mode import). La compta ne pose jamais « reçu » sur un versement : elle le **suggère** quand les produits comptabilisés dépassent les versements reçus. Raison : Gaël, 15/09 — « plusieurs sources de données, les dépenses tagguées avec le code analytique associé à une édition / action / financement » ; patron d'erp-tlst.
- 16/09/2026 — **Vue transversale → détail en panneau, champ d'origine surligné** (règle générale, Gaël). Une vue qui croise (matrice, plus tard portefeuille, plan de charge) ouvre le détail **en panneau sur la vue** (`UrlPanel`, paramètre `?ligne=`), jamais sur une autre page : on corrige, on ferme, on passe au chiffre suivant. Le champ d'où l'on vient est **surligné et focalisé** (`AutoField.highlight`, paramètre `champ=`). Le corps d'un panneau vit dans un composant partagé (`components/funding/line-panel.tsx`) pour que l'onglet et la vue transversale montrent exactement la même chose. Raison : retour de Gaël sur la matrice, 16/09.
- 17/09/2026 — **Barre latérale à deux niveaux** (proto « arbre déplié » validé par Gaël, `CRESS/MAQUETTE MENU/` pour les références). Une **section par icône** (Mon travail, Temps, Portefeuille, Demandes, Projets et financements, Échéances ; Direction et Admin pour les rôles CODIR / admin), la section de la page courante **dépliée d'après l'adresse** (une seule à la fois, trait d'arbre), les autres repliées ; bouton **Réduire** → rail d'icônes avec pastilles, choix mémorisé dans le navigateur ; rail imposé sous `lg`. L'arbre est calculé côté serveur dans `lib/navigation.ts` (droits, modules, visibilité du temps, badges) ; la barre n'affiche que ce qu'elle reçoit et reconnaît l'entrée active par préfixe d'adresse et paramètres (`?vue=`, `?section=`, `?equipe=`). **Tâches et Notes reviennent dans la barre**, sous Mon travail : dans un arbre elles ne rallongent plus rien, et les menus rapides de la barre haute gardent leur rôle (jeter un œil, créer) — raccourci vs navigation, pas un doublon. Écran café passe sous Direction (c'est l'animateur qui projette). Les sous-onglets en page (Temps, Projets et financements, Admin, Demandes) sont **conservés** : le mobile en a besoin ; on décidera en live s'ils se masquent au-dessus de `md`. Raison : demande de Gaël du 16/09 (« un système de menu de gauche double niveau »), proto choisi le 17/09.
- 17/09/2026 — **PostgreSQL** (lot P, go de Gaël). Un seul moteur pour dev, tests et serveur ; les 30 migrations SQLite du prototype sont remplacées par **une migration initiale** (`20260917000000_initial_postgres`, générée depuis le schéma) — aucune donnée réelle n'existait, la démo se reseed. Deux `contains` passent en `mode: "insensitive"` (SQLite l'était par défaut). Les tests ont **leur base** (`pilote_test`, `TEST_DATABASE_URL`) et leur port (`PW_PORT`) : ils ne touchent plus le serveur de dev. `deploy.sh` crée rôle et base une fois (mot de passe généré, gardé dans le `.env` serveur et dans `/var/www/cress-pilote.dburl`, 600), remplace la ligne SQLite du `.env`, sauvegarde par `pg_dump` (`/var/backups/cress/cress_pilote-<date>.dump`). Raison : plusieurs personnes qui écrivent en même temps, sessions de connexion à venir (lot F), sauvegardes et hébergement UE (ENF-3, ENF-7) ; SQLite verrouillait le fichier entier (contournement déjà nécessaire pour les notifications).
- 17/09/2026 — **Comptes et connexion** (lot F, go de Gaël : « email / mot de passe d'abord »). better-auth sur Prisma (`User`, `Session`, `Account` credential, `Verification`), `Person.userId` et `Person.email` relient l'accès à la personne — deux objets, Entra ID viendra comme second fournisseur sur le même `User`. Sans session, tout renvoie à `/connexion` (middleware) ; `getCurrentPerson` reste l'entrée unique et lit la session. Pas de mail dans le prototype : mot de passe oublié et création de compte déposent un lien (une heure, usage unique) dans une **boîte d'envoi** que l'admin remet à la personne. Désactiver une personne ferme ses sessions et lui refuse la connexion ; sessions 7 jours sans cache dans le cookie ; 5 essais de connexion par minute. Mode démo (`PILOTE_DEMO=1`) : « Changer d'utilisateur » reste disponible aux personnes connectées ; mot de passe de démo commun `pilote-demo-2026`. Pas de droits par module en base (lot F2). Raison : ENF-10 / EF-K4 (comptes, journal des connexions), fin du sélecteur libre « Je suis… » comme seul accès, base saine avant Entra ID.
- 17/09/2026 — **Rôles et droits en base** (lot F2, go de Gaël). Catalogue de permissions en code (`lib/permissions.ts`, 22 clés par module), rôles en base (`Role`, six rôles système + rôles créés par copie, `Person.role` en clé étrangère), niveau de validation = attribut du rôle, contexte (pilote, équipe, pôle, propre demande) en code dans `lib/rights.ts`. Pas de droits par personne : un cas particulier = un rôle. Écran Admin › Rôles et droits ; garde-fous (Direction garde l'administration, une direction active au moins, un rôle porté ne se supprime pas). Droits par défaut = ceux du prototype. Raison : EF-K1 / ENF-12 (droits réglables sans prestataire), produit à plusieurs instances (`docs/produit.md`), fin des `role === "…"` dispersés.
- 17/09/2026 — **Fiche personne et « Préparer un départ »** (lot E1, go de Gaël). Prénom / nom, fonction, téléphone, dates de présence, note sur `Person` ; fiche en panneau sur Admin › Personnes ; chacun tient sa fonction et son téléphone. « Préparer un départ » réattribue en une fois ce que la personne porte (projets pilotés, garanties, pôle, sponsor, actions, demandes), retire des équipes en cours, date et désactive — **sans rien effacer** (EF-K3). Parti pris : les contacts externes ne sont pas des `Person` (équipe = compte, temps, droits) ; ils viendront avec les organisations unifiées (lot E2). Raison : EF-K3 en liste V1, fiche jusqu'ici réduite au nom, et le produit à plusieurs instances a besoin d'un annuaire propre.
- 18/09/2026 — **Organisations unifiées** (lot E2, go de Gaël). Une table `Organisation` pour financeurs, fournisseurs, partenaires, réseaux et collectivités, genres cumulables ; contacts détachés jamais supprimés ; partenaires liés à l'édition en plus du texte. Parti pris : les relations gardent leur nom de rôle (`funder`, `supplierRef`) — c'est le rôle qui compte, pas le type ; migration de données avec identifiants conservés. Raison : produit à plusieurs instances (`docs/produit.md`), fin des trois tables pour une même réalité (TLST : `Partenaire` + `estFinanceur`), un partenaire n'est pas forcément un financeur.
- 18/09/2026 — **Contacts et listes** (go de Gaël). Un annuaire commun de contacts (personnes extérieures, organisation facultative) et des listes à soi avec visibilité, projet et **colonnes propres bornées à quatre types** (case, texte, date, liste de valeurs) ; import csv / xlsx avec correspondance des colonnes et rapprochement par e-mail ; export CSV. Parti pris : pas de tableur générique (« pas envie de recoder Airtable ») — les colonnes communes vivent dans l'annuaire, le spécifique au niveau de la liste. Brevo viendra sur cette table (identifiant et statut RGPD déjà prévus). Raison : chaque pilote tient ses Excel de contacts ; les invitations, réseaux et adhérents en dépendent.
- 18/09/2026 — **Brevo** (go de Gaël, après « Adhérents, Brevo… ne sont-ils pas une liste qui se remplit toute seule ? »). Parti pris : **la donnée qui a des règles ou de l'argent est typée** (adhésions, prêts : leurs tables, à venir) ; **la présentation en liste est générique** — une liste de contacts a une *source* : manuelle, importée, ou **miroir d'une liste Brevo** (membres et attributs tenus par la synchro, en lecture), plus tard calculée (« adhérents à jour »). Brevo et HelloAsso sont des **connecteurs** (clé dans l'environnement, jamais en base ni donnée à un agent), Adhérents / Trésorerie / Matériel des **modules activables** (`INSTANCE_MODULES`). Sens Brevo → outil pour tout le compte ; outil → Brevo liste par liste, explicite. Désinscrits et supprimés marqués, jamais effacés ni renvoyés (RGPD). Notes de frais, Entra ID, multi-instance : pas maintenant.
- 18/09/2026 — **Listes de base** (retour de Gaël). Une liste par genre d'organisation, calculée et non supprimable (« Interlocuteurs · financeurs »…), plutôt qu'une liste stockée à maintenir : la source de vérité reste le rattachement contact → organisation, la liste n'est qu'une vue exportable. Première « liste calculée » du cadre posé pour Brevo ; « adhérents à jour » suivra le même patron.
- 18/09/2026 — **Adhérents + HelloAsso** (go de Gaël). Pas de table « adhérent » : l'adhérent est une organisation de l'annuaire (genre `member`) ou un contact ; **l'adhésion est l'objet typé** (année, collège, cotisation, statut, règlement). Module activable `adherents`, droit `members.manage` (direction, RAF ; tout le monde lit). HelloAsso = connecteur à sens unique, HelloAsso fait foi pour ses items ; une adhésion préparée à la main pour le même membre et la même année est réglée par l'item HelloAsso plutôt que doublée. Les inscrits d'un événement HelloAsso suivi deviennent une liste miroir (même patron que Brevo). Suppression interdite d'une adhésion HelloAsso (elle se marque annulée). Reconduction N → N+1 = adhésions « à régler » copiées (exonérée reste exonérée).
- 18/09/2026 — **Trésorerie** (go de Gaël). Un plan calculé, pas un second système : les versements attendus, les factures et engagements, les cotisations viennent des dossiers ; seuls les flux « hors dossiers » (salaires, loyer, subvention de fonctionnement, prestations…) se saisissent, comme des règles récurrentes ou ponctuelles. Solde de départ = relevé bancaire à un mois donné. Prudence par défaut : les engagements sans date de paiement et les cotisations à régler tombent sur le premier mois. Donnée sensible : lecture direction, RAF, responsables de pôle. Le module d'erp-tlst (scénarios, rapprochement bancaire, IA) n'est pas repris tel quel : trop pour la CRESS aujourd'hui.
- 18/09/2026 — **Matériel et prêts** (go de Gaël). Un inventaire et un registre, pas une réservation : la disponibilité se calcule (quantité − sorti), un prêt se rend avec une note d'état, un matériel se sort de l'inventaire sans jamais être supprimé (le registre reste). Emprunteur = personne de l'équipe, contact ou organisation de l'annuaire, pour une édition facultative. Tout le monde enregistre un prêt ou un retour ; l'inventaire se tient avec un droit (direction, RAF, assistant·e).
- 18/09/2026 — **Trésorerie, charges à venir** (retour de Gaël : « un leasing à partir de février, une embauche en mars, où je le mets ? »). C'était possible (une ligne avec premier mois) mais pas lisible : le bouton devient « Nouvelle charge ou recette », le dialogue l'explique, et une règle existante se modifie **« à partir d'un mois »** (l'ancien montant s'arrête le mois d'avant, une nouvelle ligne prend le relais) — un salaire qui change, un loyer qui augmente, sans fausser l'historique.
- 18/09/2026 — **Fiche de prêt** (retour de Gaël). Le prêt devient une entité à part entière avec un numéro, une fiche imprimable et envoyable : la preuve du prêt. Envoi = mailto prérempli depuis la messagerie de la personne (l'outil n'envoie pas de mail ; on trace l'envoi). Une organisation emprunte toujours par une personne (contact) ; la date de retour est obligatoire ; caution = chèque (montant, référence, rendu le). Pièces jointes réutilisées (`Attachment` sans édition) plutôt qu'une table à part.
- 18/09/2026 — **Trésorerie, le réel** (retour de Gaël). Le réel vient du grand livre déjà importé (écritures datées, comptes → catégories du plan), pas d'un relevé bancaire : réel comptable, assumé et affiché comme tel ; les relevés viendront si l'usage le demande. Les dépenses récurrentes ne se reconduisent pas toutes seules : à la saisie, l'outil propose le montant « d'habitude » (réel des trois derniers mois) et la personne tranche. Les ressources humaines sont des lignes à part (kind hr) qui alimentent « Salaires et charges ».
- 19/09/2026 — **Menu : Projets / Financements / Annuaire** (retour de Gaël). Ce qu'on fait (projets, éditions, portefeuille, charge) d'un côté ; comment c'est payé (conventions, qui finance quoi, appels) de l'autre ; les organisations et contacts dans un annuaire, où l'on ne pilote rien. Un projet a une fiche ; sa liste ne dit que l'état (déduit des éditions : pas de statut à tenir à la main, seulement « archivé »). « Mission » se lit « raison d'être » : pourquoi la CRESS porte ce projet.
